#Creamos una app que segun los datos del alumno sacamos asistencias de un archivo csv
from menuPrincipal import menu_principal #Importamos la funcion "menu_principal" para ocuparlo abajo

if __name__ == "__main__": #el codigo se ejecutara solo si el script es el programa principal
    menu_principal() #Si la condicion es verdadera la funcion "menu_principal()" se ejecutara